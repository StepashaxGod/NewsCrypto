{
  "name": "newscrypto",
  "version": "1.0.0",
  "description": "- `index.html` — main page - `css/` — styles - `js/` — scripts - `assets/` — images, utils, e.t.c - `README.md` — documentation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "node-fetch": "^3.3.2"
  }
}
